import 'package:flutter/material.dart';

class Accomodations extends StatelessWidget {
  const Accomodations({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Accomodations')),
      body: const Center(child: Text('Accomodations Page')),
    );
  }
}
