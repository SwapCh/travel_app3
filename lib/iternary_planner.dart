import 'package:flutter/material.dart';

class ItenaryPlanner extends StatelessWidget {
  const ItenaryPlanner({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Itenary Planner')),
      body: const Center(child: Text('Itenary Planner Page')),
    );
  }
}
